pipeline {
    agent any
    stages {
        stage('show folders') {
            steps {
                sh 'touch .halloween'
                sh 'ls -lah .'
                sh 'pwd'
            }
        }
    }
}